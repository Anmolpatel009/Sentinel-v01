# ============================================================
# Sentinel Configuration
# Version: v1
#
# This file defines the runtime behavior of Sentinel.
# In Sentinel v1, configuration is loaded only from YAML.
# ============================================================

runtime:
  name: sentinel-v1

logging:
  level: INFO
  format: text
  add_source: true